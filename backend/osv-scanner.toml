# Backend Gradle lockfile advisory exceptions. Keep these vulnerability-id
# scoped, reasoned, and adjacent to this lockfile so new advisories still
# fail the PR gate. Remove each entry once the fixed version is adopted.

[[IgnoredVulns]]
id = "GHSA-5jmj-h7xm-6q6v"
reason = "jackson-databind 2.21.4 CVE-2026-54515 — 취약 조건 미해당(per-property @JsonIgnoreProperties 제외 + ACCEPT_CASE_INSENSITIVE_PROPERTIES 조합 미사용)이며 수정 버전 미출시(2026-07-07 기준). fix 게시 시 버전 상향으로 대체한다(#1854)."
